[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -Dspring.profiles.active=prod -jar target/*.jar"
healthcheckPath = "/api/v1/properties"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
